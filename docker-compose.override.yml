version: '3.8'

services:
  backend:
    environment:
      - ENV=development
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend:/app:rw
      - ./server_logs:/var/log/portfolio:rw
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
